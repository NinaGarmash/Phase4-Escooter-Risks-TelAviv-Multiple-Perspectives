---
description: Плановое завершение сессии по ISTRC White Paper — обновить снэпшот, дописать лог, закоммитить
---

Nina завершает рабочую сессию по проекту **ISTRC White Paper**
(`WhitePaperISTRC26/`) и хочет зафиксировать состояние так, чтобы следующая
сессия (свою или чужую — через `/istrc-resume`) могла легко подхватить работу.

Выполни по порядку:

1. Просмотри эту сессию (разговор + фактически изменённые файлы) и определи,
   что из содержательных изменений/решений ещё НЕ записано в
   `WhitePaperISTRC26/LOG.md` (если в течение сессии логирование уже шло
   по ходу дела — здесь только досоставь недостающее, не дублируй).
2. Допиши в `LOG.md` недостающие записи: дата, что сделано/решено, какие
   файлы затронуты. Формат — как в существующих записях файла.
3. Перепиши `WhitePaperISTRC26/STATE.md` с нуля (не дописывай поверх старого
   снэпшота) под текущее реальное состояние: фаза проекта, что сделано,
   открытые вопросы, последнее действие, следующий шаг.
4. Обнови `WhitePaperISTRC26/NEXT_STEPS.md`: отметь выполненные пункты `[x]`,
   добавь новые пункты, возникшие за сессию (особенно в "Требует уточнения
   с Nina", если что-то новое всплыло и осталось без ответа).
5. `git status -- WhitePaperISTRC26` — проверь, что именно изменилось.
   `git add` только релевантные файлы внутри `WhitePaperISTRC26/` (не `-A`
   по всему репозиторию — в корне репозитория много несвязанных untracked
   каталогов других треков).
6. Закоммить с содержательным сообщением (что сделано за сессию, не только
   "обновлён кит"), с атрибуцией из системного напоминания этой сессии.
7. Впиши хэш коммита в соответствующую запись `LOG.md` (короткий follow-up
   коммит с этой правкой — нормально, или впиши заранее известный хэш, если
   инструмент его вернул).
8. Дай Nina короткую сводку: что сохранено, что закоммичено (хэш/сообщение),
   что осталось открытым для следующего `/istrc-resume`.
